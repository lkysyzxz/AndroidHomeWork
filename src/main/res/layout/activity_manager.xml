<?xml version="1.0" encoding="utf-8"?>
<android.support.v4.widget.DrawerLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tool="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:id="@+id/manager_drawer"
    tool:context="edu.cqu.wwt.drawerlayout.ManagerActivity">
    <!-- 主界面 -->
    <include layout="@layout/app_bar_manager"/>
    <!-- 导航视图-->
    <android.support.design.widget.NavigationView
        android:id="@+id/manager_nav_view"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:fitsSystemWindows="true"
        app:headerLayout="@layout/app_nav_header"
        app:menu="@menu/login_menu"/>
</android.support.v4.widget.DrawerLayout>
